
export async function analyzeAssetPhoto(base64Image: string, assetType: string) {
  // Đã vô hiệu hóa để tiết kiệm chi phí
  return "";
}

export async function generateProjectSummary(assets: any[]) {
  // Đã vô hiệu hóa để tiết kiệm chi phí
  return "Tính năng báo cáo AI đã được tắt.";
}
